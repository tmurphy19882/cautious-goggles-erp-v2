"""O2C domain package — sales order, credit, reservation, invoice, payment.

Wave 1 — O2C complete.

Each submodule owns one business capability. Routers live in
`o2c.api`; the in-process orchestrator lives in `o2c.workflows`.
"""
