# docs/openapi — generated OpenAPI schema

This directory holds the published OpenAPI schema for ERP v2.

`erp.json` is generated by running:

```
cd services/erp
python scripts/dump_openapi.py
```

…and is committed to the repo so the frontend SDK generator and Kong
config can pin to a known schema. CI re-generates and diffs; merge
blocked on drift.
