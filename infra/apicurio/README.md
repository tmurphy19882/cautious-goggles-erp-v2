# infra/apicurio — schema registry placeholder.
# The image is declared in docker-compose; this directory holds any
# future config / artefacts (e.g. compatibility rules, global rules).
# Empty for W0.
